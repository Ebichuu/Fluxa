# 当前状态与变更记录

快照时间：2026-07-13 06:22（Asia/Shanghai）

## 当前服务

- fnOS 核心 Web 服务均可访问。
- Torra 已于 04:28 完成配置重载，容器运行正常。
- Emby 在线，版本 `4.8.11.0`。
- Symedia、Refind、CloudDrive2、FastEmby 与 qB 端口在线。
- Hermes Dashboard 每 30 分钟刷新。

## 当前媒体规模

- Emby：电影 4,984、剧集 7,132、单集 136,833。
- Symedia：STRM 144,754。
- Torra：订阅 194、站点 20、后台任务 5。
- Refind：统计 3,347 项，缺失 324 项，转存计划 352 条。

## 当前秒传队列

- `retry_pending`：启用，每 8 小时运行。
- 2026-07-13 00:00：重试 8 个，成功 6 个，失败 2 个。
- 主秒传目录：0 个媒体文件，启动扫描已清空积压。
- 失败目录：6 个真实媒体文件，共 6,790,365,720 字节（约 6.32GiB）；状态文件未计入。
- 失败原因：115 API `index out of bounds on dimension 1`。
- 04:26 启动扫描验证：日漫积压秒传成功；综艺积压被接管后遇到上述 115 错误，已安全移入 `06-综艺` 失败目录并登记重试。
- 06:06 分类路径迁移触发 26 个媒体处理任务：24 个秒传成功，2 个遇到同一 115 错误并安全进入失败队列；任务总线 26/26 正常结束。

## 分类接力配置

- `file_event_mover` 使用硬链接把 `/qbdownload/torra/<分类>` 接到 `/qbdownload/秒传/<分类>`。
- 8 个分类 mover、秒传配置和 Symedia 归档配置均已启用。
- `02-国产剧` mover 于 03:22 新增并验证生效。
- `06-综艺` mover 于 03:20 启用并验证生效。
- 两项源目录、目标目录、分类和 `hardlink` 方式均正确，事件监听已开启。
- qB、主秒传、秒传失败三套目录的 8 个分类根目录均存在。
- Torra 04:28 重载后，8 个 `secupload_115` 监控全部启动，启动日志没有“监控源目录不存在”。

## 最近修复

### 豆瓣榜单订阅未进入分类子目录

- 根因：豆瓣榜单插件使用总目录作为全局 `save_path`，只计算 `resolved_category` 而不拼接分类目录；qB 下载器的 `auto_category` 关闭，因此订阅和下载任务直接落到 `/torra` 根目录，绕过分类 mover。
- 订阅修复：纠正 50 条总目录、旧容器目录或空路径订阅；另将 8 条落在 `07-纪录片`、`09-欧美动画`、`99-未分类` 的订阅并入当前完整的 8 分类链路，共修正 58 条。无豆瓣历史关联的旧 `/qbdownload/AUD` 订阅保持不动。
- 持久保护：安装 4 个 SQLite 触发器，覆盖豆瓣记录新增/更新及订阅自身新增/路径写回；只修正仍为总目录、旧路径或空值的豆瓣榜单订阅，不改手工自定义路径。事务测试把《半熟恋人》强制写回总目录后立即恢复为 `06-综艺`。
- qB 迁移：通过 qB API `setLocation` 迁移 19 个已完成任务，禁止直接移动做种文件；迁移后任务仍为完成/做种状态。总目录、`07`、`09`、`99` 当前均为 0 个 qB 任务，qB 没有 `missingFiles/error`。
- 文件接力：迁移共产生 26 个 mover 任务和 26 个秒传任务，全部正常结束；24 个文件秒传成功，2 个因既有 `p115client` 越界错误进入同分类失败目录。主秒传队列已清空。
- 目录清理：迁走根目录中的一个孤立电影资源和一个未完成文件，删除 4 个空资源目录；8 个分类根在 qB、主秒传、秒传失败三套目录中全部存在。
- 服务重载：等待秒传清空后重载 Torra，8 个 `secupload_115` watcher 全部启动，无“监控源目录不存在”；订阅调度器已重新加载正确路径。
- 最终验证：豆瓣订阅错误路径 0 条，数据库 `integrity_check=ok`，4 个保护触发器均存在。
- 主要回滚备份：`/vol1/1000/tools/torra/config/backups/torra-before-subscription-path-fix-20260713-060436.db`、`torra-before-unsupported-subscription-path-fix-20260713-061003.db`、`torra-before-subscription-path-guard-20260713-061420.db`；qB 任务迁移前快照保存在同目录 `qb-*-before-path-fix-*.json`。

### 豆瓣榜单失效订阅历史

- 2026-07-13 发现最新榜单同步中有 70 条因“插件以前添加过”被跳过，但其中 37 条所引用的订阅 ID 已不存在。
- 变更前完成 SQLite 在线备份，并单独导出这 37 条记录；随后只删除当前榜单命中的 37 条失效历史。
- 重新同步结果：新增订阅 17 条、Emby 已完整入库跳过 23 条、有效插件历史跳过 33 条、评分过滤 27 条、TMDB 无法匹配 2 条。
- 新增的 17 条订阅均已在 `subscriptions` 表中确认存在，数据库 `integrity_check=ok`。
- 剩余失败只有《新攻壳机动队》和《海贼王：女英雄们的故事》，原因均为无法解析有效 TMDB 媒体。
- 回滚文件：`/vol1/1000/tools/torra/config/backups/torra-before-douban-stale-history-clean-20260713-055131.db`；单独记录备份：同目录 `douban-stale-history-rows-20260713-055131.json`。

### Torra 多名称搜索

- 老大已于 2026-07-13 关闭多名称搜索。
- 实时数据库验证：`settings.search_multiple_name = 0`，更新时间 `2026-07-13T05:06:21`。
- 后续聚合搜索不再依次使用中文名、英文名和别名重复发起搜索，可减少订阅批处理耗时及重复站点请求。

### 02-国产剧失败路径

- 发现：`temp_path` 曾错误指向 `/qbdownload/秒传失败/01-国漫`。
- 处理：老大于 2026-07-13 手动修正。
- 实时验证：2026-07-13 03:03:56，配置已变为：

```text
source_path = /qbdownload/秒传/02-国产剧
temp_path   = /qbdownload/秒传失败/02-国产剧
dest_path   = /00-待整理/02-国产剧
enabled     = true
```

### daily-reflection 路径

- 2026-07-13 已通过软链接统一 workspace 与 Hermes memory 目录。
- cron 保持原写入路径，两边内容同步。
- 2026-07-13 已将家庭影音中心只读巡检并入 `daily-reflection`，执行时间由每日 23:50 调整为 23:40；巡检结果写入当日日报并发送摘要。

### 秒传分类根目录被空目录清理器删除

- 根因：`empty_folder_cleaner` 原规则直接以 `/qbdownload/秒传` 为扫描根，分类目录为空后会被当作子目录递归删除；2026-07-13 00:00 日志明确记录删除了 `01-国漫`。
- 处理：停用原 `link空目录` 根规则及其计划，保留配置用于回滚；改为 8 条分类级规则，扫描根分别为 `/qbdownload/秒传/<分类>`。
- 计划：每 12 小时运行一次，8 个分类错峰在第 15 至 22 分钟执行。
- 原理：插件只删除配置根目录下面的子目录，不删除配置根目录自身，因此可继续清理空资源目录，同时永久保留分类入口。
- 目录恢复：补齐主秒传和失败目录的 8 个分类根；另补齐缺失的 qB `05-港台剧` 分类根。
- 验证：Torra 数据库 `integrity_check=ok`；8 条分类规则和 8 条计划均启用；旧根规则停用；三套分类目录完整；8 个秒传监控全部启动。
- 回滚备份：fnOS `/vol1/1000/tools/torra/config/backups/torra-before-empty-cleaner-fix-20260713-042544.db`，同时保存了变更前清理配置 JSON。

## 当前待办

1. 观察下一轮 08:00 retry 后 4 个失败文件是否减少，重点关注 `index out of bounds on dimension 1`。
2. 处理 Hermes `秒传完删种` 在 00:09 出现的 `Broken pipe`，确认是输出管道问题还是任务未执行。
3. 为 Emby/Symedia 增加每日新增快照，才能精确回答每天最终入库多少电影、剧集和单集。
