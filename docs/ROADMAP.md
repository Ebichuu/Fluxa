# Fluxa 功能路线图

状态：持续维护

更新时间：2026-07-31

本文只记录尚未完成或仍需实机确认的能力。当前架构与已完成功能见 `README.md` 和 `docs/FRAMEWORK.md`，延期能力不等于删除。

## 1. 当前完成度

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| Python 单后端 | 已完成 | Flask / Gunicorn 单容器 |
| React 与影院大厅 | 已完成 | Mineradio 主视觉保持；导航和媒体抽屉已优化，移动端开合已验收 |
| 内容发现 | 已完成 | TMDB、JustWatch、豆瓣、国内平台、全球日播 |
| NasEmby 订阅业务 | 已完成 | 列表、保存、详情、日历、分类、改季 |
| SQLite 唯一台账 | 已完成 | schema version 6；订阅、发现候选、RSS 与任务证据各自持久化，不双写状态；任务历史只保留明确结果事件，瞬时轮询只存在于当前投影 |
| 私人 PT RSS 种子库 | 当前版本已完成 | M-Team、HDHome、织梦、青蛙 RSS 2.0 字段、电影/单集/整季包和 `720p/1080i/1080p/2160p` 版本组合已进入回归；来源管理、FTS5、退避、双并发、记录上限和发现/订阅本地 RSS 搜索已完成；真实收集默认关闭 |
| Torra 读取与安全推送 | 代码完成，待实机 | v2 预览、确认、幂等、冷却和脱敏审计 |
| Torra 追更洗版 | 阶段 A-B、C0、E0-E2 完成，真实下发受阻 | RSS 候选绑定、规则影子评分、可靠基线与冠军已完成；默认停止定时整订阅搜索，控制室可读搜索批次；缺集兜底默认关闭并按单订阅、全局并发 1 执行分析，不自动下载 |
| qB、115、Symedia、Emby 任务证据 | 代码完成，待实机 | qB 全局活跃数与媒体结果已分离；115 无直接结果时只标记相邻证据 |
| qB 暂停/恢复 | 代码完成，待真实动作 | 需要用户指定测试任务 |
| Emby 证据刷新 | 代码完成，待真实动作 | 需要较新 Symedia 证据 |
| 活动日志与 NAS 指标 | 已完成 | 最近 100 条活动；指标 30 秒服务端缓存；自动测试日志已隔离 |
| 原 NasEmby 核心接口 | 源码已恢复、默认关闭 | 旧总开关继续关闭，逐接口能力矩阵保留 |
| Telegram 网盘订阅/获取 | 延期 | 底层源码、v2 契约中的延期路由和模拟测试保留，当前 React 不展示 |
| MoviePilot PT 备用 | 阶段 7 完成，待实机 | 复用 NasEmby 源码；独立开关默认关闭，仅提供观察到期后的人工预览/推送，不承担周播剧升级主线 |
| 全局作品搜索与作品总览 | 已完成 | `/api/v2/search` 聚合追更、任务、日历、已识别 RSS、Emby 与 TMDB 只读补充；`/media/:type/:tmdbId` 展示六阶段生命周期 |
| fnOS 正式部署 | 已安装，待本版实机复验 | 生产桥接和受控历史基线初始化代码已完成；先核对一天影子收据，再明确启用正式桥接 |
| 单条真实订阅闭环 | 代码接入完成，待实机 | 新 qB 完成可建立观察单元，Symedia 成功可推进基线；仍需一条新下载和一批历史初始化复验 |
| 候选来源调度 | 未启用 | 只更新发现候选，不创建追更或触发获取 |

## 2. 当前下一阶段：候选镜像与 fnOS 实机

1. M-Team、HDHome、织梦、青蛙四个真实结构脱敏夹具已满足当前版本；后续新增站点样本只扩展兼容性，不阻塞部署。
2. 四个来源的 RSS 2.0 字段、电影/单集/整季包、多版本、文件大小、enclosure 和 `720p/1080i/1080p/2160p` 已由脱敏夹具复核；`429/Retry-After`、指数退避、并发 2 和抓取记录上限已经完成。
3. 临时 SQLite 原子替换式迁移演练已经完成；本次 fnOS 没有旧生产数据，首次部署直接初始化空库，Docker 重启持久化已经通过。
4. Torra 幂等、冷却、外部 job ID 和按集追更洗版状态已持久化；洗版分析/下载/job 适配器与统一动作查询已完成。
5. RSS 阶段 A 已完成：候选绑定到可靠 Torra 订阅和规范季集范围，自动入口读取 Torra 只读规则并在 Fluxa 影子评分，不再提交整订阅分析或下载。
6. 观察单元按首次下载真实时间回扫首版下载期间到达的 RSS；E02-E03 范围包共享一个产物和一次评估，所有权冲突整体阻断。
7. 规则不唯一、身份/范围不明、评分字段不足时固定为“暂未确认”，不按零分处理；人工 Torra 整订阅分析继续作为显式兜底。
8. RSS 阶段 B 已完成：当前版本只接受 Torra 明确季集文件或具有精确身份的 Symedia 成功结果，qB 只能补充精确同名文件大小；跨批次冠军和范围包完整覆盖约束已落地，种子库按季集候选组展示真实决策。
9. 阶段 C0 已完成只读预检：Fluxa 不使用无订阅 ID 的 Torra 通用下载器入口，也不把整订阅分析结果冒充 RSS 精准下载；Torra 提供正式订阅绑定资源入口前，真实精准下发和阶段 D 自动洗版保持关闭。
10. 阶段 E0-E2 已完成：默认 `follow_rss` 停止固定窗口整订阅搜索；控制室只读展示正式 Torra 批次/调度证据；可选缺集兜底只消费可靠日历缺集，按单订阅进入既有动作台账并与下载共享全局 Torra 动作槽 1。
11. 当前源码回归数量以 `services/nasemby-core/README.md` 的最近一次完整测试为准；75 条 v2 机器契约和生产构建继续作为提交门禁。已完成首页可信事实、RSS 订阅绑定与影子评分、可靠基线与冠军、精准下载止损、默认跟随 RSS、Torra 搜索证据读取和缺集单订阅兜底。正式 RSS 与实机写动作仍需发布后受控验证。
12. 阶段 F 的代码阻断已解除：任务快照持久化后进入版本化桥接，影子模式只生成收据，正式模式在同一事务内应用观察单元；历史证据通过最多 200 项的持久预览、明确确认和原子批次初始化。阶段 F 仍待 fnOS 一天影子核对、一条新下载桥接、一次 Symedia 基线推进和一批受控历史初始化后才能完成。

剩余实施顺序以本路线图、`docs/CORE_API_CAPABILITY_MATRIX.md` 和 `docs/DEPLOYMENT.md` 为准。

## 3. 后续：PT 主链实机闭环

主线固定：

```text
媒体控制中心订阅
  → Torra
  → qBittorrent
  → Torra 秒传 115
  → Symedia
  → Emby
```

实施顺序：

1. fnOS 单容器只读部署。
2. 核对 Torra 下载器 ID、八分类保存路径和 115 秒传目标。
3. 创建一条电影测试订阅，只读取 Torra 推送预览。
4. 用户单独批准后开启一次 Torra 推送。
5. 观察 qB 下载、Torra 秒传、Symedia 接管和 Emby 索引证据。
6. 电影闭环成立后验证一条剧集季。
7. 最后开启订阅调度。

当前不执行以上实机动作。

## 4. MoviePilot 其他 PT 站点备用

MoviePilot 不是 Telegram 网盘通道。后续目标：

- Torra 追更洗版和版本控制永远优先。
- MoviePilot 自身数据库稳定性不作为中控可修复问题，因此默认关闭。
- 只有用户明确开启、Torra 不覆盖目标站点或观察已经结束，且没有 Torra/qB 活动任务时，才允许 MoviePilot 参与。
- MoviePilot 下载继续进入同一 qB → 115 → Symedia → Emby 主线。
- 不允许 Torra 与 MoviePilot 对同一媒体并行下载。
- 使用现有 `moviepilot_status()`、`moviepilot_subscribe()`，并以 [MoviePilot 官方文档](https://wiki.movie-pilot.org/) 核对接口，不猜测。
- 本轮已补齐安全预览和人工备用入口；MoviePilot 自动模式不实施，真实动作仍需受控实机窗口。

## 5. Telegram 网盘能力（延期但保留）

未来范围仍包括：

- Telegram 登录、频道选择和频道资源订阅。
- HDHive / pansou 授权、搜索、签到和影巢解锁。
- 115 分享链接候选预览与单条转存。
- PT 失败后的人工或自动网盘兜底。
- 真实 115 转存直接证据、失败对账和防重复。
- 115 清理和助力；这两项风险较高，继续排在最后。

保留边界：

- Python 模块、原路由、v2 安全适配层和模拟测试不得删除。
- 当前 React 不引用这些动作接口。
- 所有环境闸门保持关闭。
- 不启动 NasEmby `legacy/ptto115.py`；PT 主线秒传由 Torra 独占。
- 未来重新启用前必须另写设计并重新核对 Torra、qB、115、Symedia 和 Emby 重复证据。

重新启用前应基于当前接口和安全边界单独设计，不沿用已归档的迁移步骤。

## 6. 其他未完成工作

### 任务链证据

- 五类异常分类和“为什么 / 下一步”展示已完成；真实重试资格仍留在后续安全动作阶段。
- 获取 Torra 秒传到 115 的直接结果；接口没有该证据时继续明确标记推断。
- 无法可靠关联的外部记录继续显示未关联，不猜测合并。
- 实机确认八分类在 qB 保存路径、115 待整理目录和 Symedia 归档之间一致。

### 受控动作

- qB 暂停/恢复真实任务验证。
- Emby 证据刷新真实验证。
- Torra 单条真实推送与冷却验证。

### 部署

- fnOS 单容器只读部署。
- HTTPS 反向代理和源站端口限制。
- 持久目录备份、升级和回滚演练。
- 多 worker 或多副本前增加调度选主和台账并发控制。

## 7. 明确不做

- 不建立网页播放器；播放交给 Emby 客户端。
- 不建立 PT 站点爬虫或第二套质量评分器；PT 搜索和版本高低判断交给 Torra，MoviePilot 只作可关闭备用。
- 不让 NasEmby `ptto115.py` 与 Torra 秒传同时运行。
- 不向 Symedia 重复推送订阅；Symedia 只负责 115 后整理入库。
- 不把 NasEmby 原静态管理页面作为第二套用户界面启用。
- 不建立第二套订阅台账或第二套调度器。
