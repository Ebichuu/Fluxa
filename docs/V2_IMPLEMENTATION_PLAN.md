# 媒体控制中心 v2 实施计划

> v2 迁移基线计划。当前 PT 主线收口由 `docs/superpowers/plans/2026-07-17-pt-primary-control-center-implementation-plan.md` 继续执行。

状态：v2 基线和常用核心接口迁移完成，等待实机验证
日期：2026-07-17

## 当前目标

把原仓库最新 Python 统一后端、React 前端、测试、Docker、Mineradio 资源和有效参考资料迁入 `D:\Projects\媒体控制中心v2`，形成不带旧 Git 历史的唯一开发目录。核心源码执行“保留优先”：旧页面可以不启用，接口可以默认关闭，但没有替代证据前不得删除。

## 阶段与验收

### 阶段 0：迁移基线

- [x] 确认源提交为 `7368790`。
- [x] 确认 v2 使用全新 Git。
- [x] 确认原目录保留为归档。
- [x] 确认实机测试暂缓。

### 阶段 1：最新源码与资料迁入

- [x] 复制当前受控源码、测试、构建和部署文件。
- [x] 复制 Mineradio 运行资源。
- [x] 整理解压后的维护资料到 `docs/references/`。
- [x] 排除 `.env`、运行数据、缓存、构建产物和重复 ZIP。

### 阶段 2：核心源码恢复与隔离

- [x] 恢复 Python `app/main.py` 中原 NasEmby 核心路由和准确调用关系。
- [x] 保留订阅、发现、provider、115、Telegram、HDHive / pansou、Symedia 等业务模块。
- [x] 使用 `MCC_PRESERVED_CORE_API_ENABLED=false` 默认隔离尚未统一接入的原接口。
- [x] 建立逐接口能力矩阵，记录用途、底层函数、副作用和替代状态。
- [x] 将 NasEmby 原静态管理页面源码作为迁移参考放入 v2，但不注册为第二套生产页面。
- [x] 只清理缓存、构建产物、真实凭据和确认重复的压缩包；不再删除业务源码。
- [x] 建立 `V2_SOURCE_INVENTORY.md`，确认原提交 152 个文件无未找到项。

### 阶段 3：测试与契约收口

- [x] 更新源码结构测试。
- [x] 保持 47 条接口、42 条认证路由和写接口 Origin 契约。
- [x] 保留原核心接口并确认默认返回 `503 PRESERVED_CORE_API_DISABLED`。
- [x] 为常用原接口补齐模拟契约测试和 13 条 v2 映射；原静态页面不在生产端口注册。
- [x] 保持默认写入、调度、原核心兼容入口和 Torra 推送关闭。

### 阶段 4：当前文档重写

- [x] 重写 README、PLAN、FRAMEWORK、API、DEPLOYMENT 和实现来源。
- [x] 更新 Python 模块 README、DESIGN 与安全说明。
- [x] 将迁移流水账放入参考快照，保留未完成计划、原接口说明和旧 UI 调用参考。
- [x] 在计划中单独列出以后实机阶段。
- [x] 建立 `ROADMAP.md`，保留所有尚未完成的代码能力。
- [x] 建立网盘订阅详细计划，固定 PT 优先、网盘开关控制和默认关闭自动兜底。

### 阶段 5：全量本地验收

- [x] Python 83 项测试全部通过。
- [x] 前端类型检查和生产构建通过，npm 审计为 0 个漏洞。
- [x] Docker 单容器镜像构建与隔离冒烟通过。
- [x] 质量、安全、凭据和文档引用检查通过。
- [x] 从 v2 最终镜像启动独立本地预览 `http://127.0.0.1:18789/`。

### 阶段 6：交付

- [x] 更新本计划，保留逐接口迁移、网盘通道和实机阶段未完成项。
- [x] 提交 v2 全新代码基线 `4112c22`。
- [x] 后续开发以 v2 为主目录，原目录只读保留。

## 以后再做

fnOS 安装、真实订阅、Torra 单条推送、PT 完整链路和调度器开启等待用户明确进入实机窗口后再执行。

## v2 收口后的下一项代码开发

手动网盘通道的代码已经完成，项目仍不能标记为实机完成。下一步按 `docs/CLOUD_ACQUISITION_PLAN.md` 继续：

1. 在 fnOS 只读部署并配置服务状态，保持写闸门关闭。
2. 选择单条订阅验证 PT 主链。
3. 用户明确批准后开启网盘搜索，先验证脱敏候选。
4. 再单独开启一次网盘转存，验证重复阻止、幂等和 Symedia / Emby 证据。
5. 自动兜底后台执行器最后实现，且初始继续关闭。
