# 友好文案实施计划

日期：2026-07-17

## 范围

- [x] 顶部品牌副标题改为中文。
- [x] 总览、发现、订阅、任务中心、日历、控制室、订阅设置和系统设置统一中文短标与业务标题。
- [x] 任务匹配状态转换为用户语言，保留后台字段不变。
- [x] 卡片英文小标和确认框英文信号改为中文。
- [x] 影院大厅对外媒体库状态改为中文。
- [x] 默认允许复制正文、任务、错误、路径和代码，仅交互控件禁止选择。
- [x] 同步 `UI_STANDARD.md` 与 `PLAN.md`。
- [x] 完成类型检查、生产构建、文案残留扫描和变更校验。
- [x] 在 1440×900 桌面和 390×844 手机尺寸预览关键页面；本轮未改变布局断点，1024×768 沿用上一版已通过的布局验收。
- [x] 提交可回退的 Git 版本（本计划随提交一并归档）。

## 验证结果

- TypeScript 类型检查通过。
- Vite 生产构建通过，共转换 1592 个模块。
- Python 回归测试 85 项全部通过。
- `npm audit --omit=dev` 为 0 个漏洞。
- 变更校验通过，文档已同步。
- 浏览器无控制台错误；正文和标题 `user-select: auto`，按钮 `user-select: none`。

## 验收标准

1. 工作页首屏不再出现英文研发式 eyebrow。
2. 普通用户不理解外部工具名时，也能知道内容当前处于订阅、下载、115、整理或入库的哪一步。
3. 服务卡、详情和推送前检查仍能看到 Torra、qB、Symedia、Emby、TMDB 等真实依据。
4. 影院大厅核心视觉、订阅台账、API 契约和外部写入闸门没有变化。
5. 页面中的任务名、错误信息和路径可以选择复制。
