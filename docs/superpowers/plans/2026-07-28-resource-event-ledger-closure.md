# 资源事件台账收口实施计划

设计依据：`docs/superpowers/specs/2026-07-28-resource-event-ledger-closure-design.md`

方案：按已批准的方案 A 增量收口现有台账，不建立第二套事实表或统一事件总线。

## 实施波次

1. 已完成：永久事实白名单、失败幂等键、文件单元事件写入和瞬时事件一次性清理迁移。
2. 已完成：历史首次 Emby 命中投影、最终结果重新派生和兼容字段单向刷新。
3. 已完成：Symedia 单文件真实原因与多文件原因分组。
4. 已完成：按上海自然日查询持久 Symedia 文件成功事件，并与当前快照统一身份合并去重。
5. 已完成：追更互斥构成统计、日历稳定总量字段和对应前端解释。
6. 已完成：539 项 Python 回归、70 条 v2 机器契约、前端生产构建、Compose、浏览器深浅主题与 390px 移动端验收。
7. 待执行：提交并推送 `main`，等待多架构镜像、烟测与 `latest` 提升，核对不可变镜像摘要。

## 实例验收修订波次

1. 待完成：历史归档移除 `observedAt` 回退，并覆盖旧事件只有观察时间时不计入归档。
2. 待完成：迁移失败改为仓储初始化硬失败，阻断 v2 任务快照服务注册与容器启动。
3. 待完成：Symedia 普通页面错误固定为安全用户文案，补齐相对路径残片回归。
4. 待完成：追更互斥统计改为“全库构成（N）”。
5. 待完成：运行相关回归、全量 Python、机器契约、前端构建与变更校验后提交发布。

## 发布门槛

- Python 全量回归、70 条 v2 机器契约和前端生产构建全部通过。
- 迁移备份失败不删除，审计失败整批回滚，第二次执行删除数为 0。
- 相同失败轮询不增长；新一次真实失败增长；`not_applicable` 可在重启后恢复。
- 混合 Symedia 批次按成功文件归档；历史/当前同文件只计一次；离线时历史仍可读取。
- 追更五类构成总和等于总数；日历已关联、未关联之和等于合计。
- 不提交或删除 `services/nasemby-core/mcc_data.db`，不执行系统关机。
